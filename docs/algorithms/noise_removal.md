# ノイズ除去

## 概要

3DGSデータからノイズを除去する処理です。

## 手法

### 2次元DBSCAN (`denoise_2d_dbscan`)

2次元平面上でDBSCANクラスタリングを行い、最大のクラスタを抽出します。 \
主に 3dgs を扱う初期の段階で使うノイズ除去です。

### 3次元密度フィルタ (`denoise_3d_density`)

密度の低い点を除去します。 \
幹抽出の前に、密度の低い葉を取り除くために使います。
